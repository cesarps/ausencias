<p id="notice"><%= notice %></p>

<h1>Ultima Consulta</h1>
<!--
<table>
  <thead>
    <tr>
      <th>Data ultima consulta</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

      <tr>
        <td></td>
        <td></td>
        <td></td>

      </tr>

  </tbody>
</table>
-->

<p>
  <strong>
    Data da ultima consulta:
  </strong>
  <% @ultima_consulta.each do |uc| %>
      <%= uc.data_ultima_consulta.strftime("%d/%m/%Y") %> <%= link_to 'Mosrar', uc, class: 'btn btn-sm btn-primary active'  %> <%= link_to 'Editar', edit_ultima_consultum_path(uc), class:'btn btn-sm btn-default' %>
  <% end %>
</p>


<br>

<% if @ultima_consulta.count == 0 %>
    <%= link_to 'Ultima consulta', new_ultima_consultum_path %>
<% end %>

<%=link_to "Voltar à tela de ocasiões",root_path, class: 'btn btn-info active' %>
